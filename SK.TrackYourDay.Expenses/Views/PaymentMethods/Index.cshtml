@model IEnumerable<SK.TrackYourDay.Expenses.Models.ViewModels.PaymentMethodVM>
@{
    ViewData["Title"] = "Index Payment Methods";
}


<h1>Expenses Tracker</h1>
<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">All Payment Methods List</h2>
        </div>
        <div class="col-6 text-right">
            <a asp-controller="PaymentMethods" asp-action="Create" class="btn btn-primary">Create a new payment method</a>
        </div>
    </div>
    <br />

    @if (Model.Count() > 0)
    {
        <table class="table table-bordered table-striped" style="width:100%">
            <thead>
                <tr>
                    <th>
                        Payment Method Name
                    </th>
                    <th>
                        Created by
                    </th>
                </tr>
            </thead>
            <tbody>
                @foreach (var paymentMethod in Model)
                {
                    <tr>
                        <td width="30%">@paymentMethod.Name</td>
                        <td width="30%">@paymentMethod.User</td>
                        <td>
                            <div class="w-100 btn-group" role="group">
                                <a asp-controller="PaymentMethods" asp-action="Update" asp-route-Id="@paymentMethod.Id" class="btn btn-primary mx-1">Update</a>
                                <a asp-controller="PaymentMethods" asp-action="Delete" asp-route-Id="@paymentMethod.Id" class="btn btn-danger mx-1">Delete</a>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    }
    else
    {
        <p>No expense categories created yet</p>
    }
</div>
